## OpenClass-Box v0.1.3 Beta

开源实用工具箱 · 装机 / 教室 / 机房常用功能一站集成。安装包在下方 Assets 中（内嵌 WebView2 运行时、OpenOffice 便携版与 mpv，装完即用，无需额外安装任何运行库）。

### 本版亮点

**界面与导航**
- 导航顺序按使用频率重排：系统状态、硬件信息置顶，安全 / 设置 / 关于置底
- 「一键体检」「诊断」并入**维护**；「插件」并入**工具箱**；「更新」并入**设置**
- 右下角固定显示版本号；启动页换成品牌动画

**系统状态**
- CPU / 内存 / 存储改为实时**圆环可视化**（占用高自动转橙 / 红），网络保留上下行动态曲线
- 右上角自动识别**设备型号 + 主机名 + 登录用户**，多机环境一眼分辨
- 采集提速：慢查询转后台，首屏不再等待

**硬件信息（新板块）**
- CPU / 显卡 / 内存条 / 物理硬盘 / 主板 / BIOS / 温度，全部本机实测，读不到的项明确标注「不可用」
- 修复显卡显存被截断的问题（8GB 显卡此前显示 4GB，现读注册表真实容量）

**维护与诊断**
- 一键体检、一键修复（DNS / 临时文件 / 音频服务 / 网络重置）、磁盘清理、网络诊断
- 软件目录、系统还原点、报修信息导出、诊断包打包（系统信息 + 体检 + 事件日志）
- 进程 / 服务 / 启动项视图（系统关键进程拒绝结束）

**壁纸**
- 图片 / 动图 / 视频壁纸，位置（填充/适应/拉伸/平铺/居中/跨屏）与大小可调，支持直接导入
- 视频与动图由随包 mpv 渲染到桌面层；承载窗口四级兜底，兼容各版本 Windows 桌面结构

**安全防护**
- 打开网页即自动检测（读 Chrome / Edge 本地历史，只读不外传）；剪贴板网址同样自动评分
- 风险记录、白名单、开关；只提示，不拦截任何操作

**工具集成**
- Apache OpenOffice **随包携带**（中文路径自动迁移到英文目录运行，实测可用）
- 7-Zip / VLC / LibreOffice 自动发现本机安装版或便携版
- 右键「用 OpenClass-Box 打开」，点击后由工具箱内对应工具处理该文件

**系统与可靠性**
- 设置持久化：安装在受保护目录时自动回退用户目录，保证设置一定保存成功
- 单实例：重复启动会唤起已有窗口；关闭默认最小化到托盘（可在设置中改为直接退出）
- 修复最大化 / 最小化状态错乱、托盘唤起、同名校验（LibreOffice 误指 OpenOffice）等问题
- **代码签名**：exe 与安装包已签名（自签名证书），安装时可勾选「在本机信任签名证书」消除「未知发布者」提示

### 安装说明

1. 运行 `OpenClass-Box_Setup.exe`（向导第一步为用户协议，需勾选「我接受」）
2. 「系统集成」建议保留勾选：右键菜单 + 在本机信任签名证书
3. 安装完成直接启动即可

### 已知说明

- 本版使用**自签名证书**：陌生电脑首次运行仍会提示「未知发布者」，正式证书（SignPath）通过后会换签
- 视频壁纸依赖随包 mpv（已内置）；CPU / 主板温度依赖硬件监控驱动，部分机型读不到属正常现象
- 项目仓库：https://github.com/HMUG12/OpenClass-Box
