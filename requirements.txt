# ── 桌面宿主 ──
pywebview>=6.0
# Windows 下的 WebView2 绑定（pywebview 的 edgechromium 后端依赖）
pythonnet>=3.0; sys_platform == "win32"

# ── 系统监测 ──
psutil>=5.9.0
requests>=2.31.0

# ── 打包（可选） ──
pyinstaller>=6.0
